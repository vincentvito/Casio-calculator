/// Neumorphic surface styles
enum NeumorphicStyle {
  /// Raised surface with outer shadows (default button state)
  convex,

  /// Pressed surface with inner shadows (pressed button state)
  concave,

  /// Flat surface with no shadows
  flat,
}
